# Basic Agent (RAG + Web Search)

This agent exposes a single /ask endpoint. It can use the RAG tool (local documents) or a web search tool.
Run it alongside the Simple RAG example (they share the same Chroma store).
